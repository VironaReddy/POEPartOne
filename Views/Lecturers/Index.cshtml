@model IEnumerable<POEOne.Models.Lecturer>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LecturerName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LecturerEmail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HoursTaught)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Month)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LecturerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LecturerEmail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HoursTaught)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Month)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LecturerID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LecturerID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LecturerID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
