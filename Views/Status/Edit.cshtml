@model POEOne.Models.Status

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Status</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ClaimId" />
            <div class="form-group">
                <label asp-for="LecturerID" class="control-label"></label>
                <select asp-for="LecturerID" class="form-control" asp-items="ViewBag.LecturerID"></select>
                <span asp-validation-for="LecturerID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ClaimApprovalId" class="control-label"></label>
                <select asp-for="ClaimApprovalId" class="form-control" asp-items="ViewBag.ClaimApprovalId"></select>
                <span asp-validation-for="ClaimApprovalId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Approve" class="control-label"></label>
                <select asp-for="Approve" class="form-control"></select>
                <span asp-validation-for="Approve" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
